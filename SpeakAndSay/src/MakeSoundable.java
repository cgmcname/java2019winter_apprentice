
public interface MakeSoundable {
	
	/**
	 * The makeSound method is the template for the function calls in the different classes
	 * @author Christopher McNames
	 * 
	 * @return
	 */
	 String makeSound();
	 
	 String accessClassName();

}
